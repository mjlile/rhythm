proc power (a Int, n Int) Int {
    acc Int <- 1
    while n > 0 {
        acc <- acc * a
        n <- n - 1
    }
    return acc
}

proc main() Int {
    n Int <- 10
    if n = 10 {
        n Int <- 2
        n <- 3
    }
    a Int <- power(3, 3)
    return a + n
}
