proc main() Int {

    n Int <- 0
    printf("n = ")
    scanf("%d", address(n))
    printf("2n = %d\n", 2 * n)
    ptr Pointer(Int) <- address(n)
    printf("%p -> %d\n", ptr, deref(ptr))

    return 0
}
