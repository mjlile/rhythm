let x : Int <- 10
let y : Str <- x+1*2-8/2
let z : Int <- 0

println(z)
if z {
    println(z)
}

proc power (let a : Int, let b : Int) -> Int {
    let result : Int <- 1
    while b {
        result <- result * a
        b <- b - 1
    }
    return result
}

if x {
    z <- z + 1
    println(x)
}

println(x)

while x {
    println(x)
    x <- x - 1
}

println(y)
println(z)
println(power(y, 3))
